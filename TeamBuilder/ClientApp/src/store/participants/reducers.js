import { } from './actionTypes';

const initialState = {
};

export const participantReducer = (state = initialState, action) => {
    switch (action.type) {

        default: {
            return state;
        }
    }
};
